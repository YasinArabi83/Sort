
 @{
    ViewBag.Title = "مقایسه سرعت ";
 }   


@model List<DllModel>

<table class="table table-striped table-hover container">
    <thead>
        <tr>
            <th scope="col">اسم</th>
            <th scope="col">سرعت اجرا(ms)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            
            
           <tr>
                <th scope="row">@item.Name</th>
                <td>@item.Time</td>
           </tr>
        }

        
        
    </tbody>
</table>